
/**
 * @typedef {Provdier}
 * A function that returns random data when called.
 * @todo Copy from README
 */

/**
 * @typedef {ProviderCreator}
 * A function that creates a provider.
 * @todo Copy from README
 */

/**
 * @typedef {Repeater}
 * A repeater is a function that accepts a function and a number of times to repeat.
 * The result is an array.
 */
